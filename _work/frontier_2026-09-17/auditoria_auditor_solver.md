## Taxonomia de `solver/` (407 arquivos rastreados, 3.294 KiB)

**Estado da árvore no momento da auditoria:** `solver/` está intacto, mas outro processo está reorganizando docs em paralelo (`ENDGAME.md → docs/historico/ENDGAME_cronologico_2026.md`, `docs/README.md`, `docs/CODEX-NAVIGATION-GUIDE.md`, `docs/research-manifest.json` e `.claude/skills/gsmg-solver/SKILL.md` apagados no índice, `_work/*.jsonl|.ps1` soltos apagados). Referências a docs abaixo são do `HEAD`.

**Fatos estruturais que decidem "mover ou ficar":**
- `solver/oracles.py` (`ROOT = dirname(dirname(__file__))` → lê `README.md`) e `solver/scorer.py` (mesmo `ROOT` → `result.json`; grava `quadgram.pkl` ao lado, ignorado) **precisam estar exatamente em `solver/<arquivo>.py`**.
- `solver/experiments/claude_endgame_2026_09_02/gsmg_common.py` tem `SOLVER = r"C:\Users\ruthe\Desktop\puzzle\gsmgio-5btc-puzzle\solver"` **absoluto** (linha 9) e 45 scripts fora do diretório fazem `sys.path.insert(0, r"C:\...\solver\experiments\claude_endgame_2026_09_02")` **absoluto**; os 77 `.py` do snapshot usam `SP/SCR = dirname(__file__)` ou o mesmo absoluto. Mover o kit = quebrar ~120 scripts.
- Todos os 110 `.py` da raiz importam vizinhos por nome de módulo após `sys.path.insert(0, dirname(__file__))`; 25 gravam em `_work/<nome>.jsonl` via `dirname(dirname(__file__))` (assumem profundidade 1).
- 179/182 `.cjs` resolvem a raiz como `path.resolve(__dirname, '..')`, 94 leem `_work/prime_geometry_2026-09-11/inputs.json`, todos leem/gravam em `_work/<campanha>_<data>/`, e 47 são `require('./x.cjs')` por irmãos (`decimal_keystream_constraints.cjs` é hub de 45; `zero_decimal_constraints.cjs` de 16). O guia Codex (`HEAD:docs/CODEX-NAVIGATION-GUIDE.md`) diz explicitamente: *"Muitos scripts possuem caminhos fixos para `_work/`; não mova uma campanha apenas para limpar a raiz."*
- **Não há jsonl/log/pkl rastreado em `solver/`.** `solver/out/`, `quadgram.pkl` e as saídas do snapshot estão ignorados (`.gitignore` do snapshot: `*`, `!*.py`, `!*.md`). O único arquivo de saída versionado é `solver/gpu_aes16_dbbi_hex/validate_result.json` (1.311 B).

Proveniência (commit de adição): 264 arquivos (169 `.cjs` + 92 `.py` + docs) entraram em `ff18d8d` (2026-09-16, "organiza pesquisa"); os `.cjs` são as campanhas Codex, cada uma com `_work/<campanha>_<data>/RELATORIO.md` (69 relatórios indexados em `docs/RESEARCH-INDEX.md`). Os `.py` da raiz vieram commit a commit entre 2026-07-14 (`0dda940`, harness) e 2026-09-17.

---

### (a) Tabela por grupo

| Grupo (caminhos reais) | Papel | Família | n | Dependências (importa → / ← importado por) | Seguro mover? |
|---|---|---|---|---|---|
| `solver/oracles.py`, `scorer.py`, `dsl.py`, `search.py` | **KIT/ORÁCULO** (núcleo) | oráculos duros AES/privkey/BIP39; quadgramas; DSL Bifid | 4 (24 KiB) | `oracles` ← **82** scripts; `scorer` ← 33; `dsl` ← 14; `search` ← 7. `oracles` → `README.md`, base58/ecdsa/bip_utils/Crypto; `scorer` → `result.json` (27 MB, não versionado); `dsl` → oracles; `search` → dsl, oracles | **NÃO** (ROOT por `__file__`; `gsmg_common` aponta para `solver/` por caminho absoluto) |
| `solver/experiments/claude_endgame_2026_09_02/gsmg_common.py` | **KIT ativo** | dados canônicos, blobs SMALL/COSMIC/TAIL32, EVP, `try_password_all`, checkerboard/Bifid, scorer | 1 (20 KiB) | → `oracles` (abs), `scorer` (lazy), Crypto, coincurve, base58 · ← **104** (77 snapshot + 20 raiz + 9 premissas + 9 primos + `gpu_aes16_dbbi_hex/refkit.py`) | **NÃO** |
| `solver/experiments/claude_endgame_2026_09_02/` (restante: 76 `.py`, `BRIEFING.md`, `README.md`, `.gitignore`) | CAMPANHA FECHADA (rodadas 1–3 de 2026-09-02; 0 hits) + kits internos | 18+13 famílias do BRIEFING: bifid3x3, checkerboard_keystream, permutation_search, residue_autocorr, yinyang_interleave, brainwallet, hashthetext, kdf_variants, matrix_*, phase2_*, png_stego, running_key, select_bits, symbol_map, tail32_history, telegram_miner… | 79 (780 KiB) | kits internos importados por irmãos: `permutation_search.py` ← 4, `permutation_search2.py`, `brainwallet.py`, `select_bits.py`, `seed_encoding_in_bif.py`, `yinyang_interleave_pass1.py` ← 1 cada; `bifid3x3_exhaustive.py` é `exec()`ado por `bench3.py`. Leem `_work/cosmic_duality.txt`, `_work/looking_forward.txt`, `_work/archive/*.png` (absolutos) e jsonl/pkl próprios (não versionados) | **NÃO** individualmente; o diretório inteiro só com sed dos 45 absolutos externos |
| `solver/final_chain.py`, `solver/strong_oracle_35.py` | KIT **da cadeia refutada** | Chain1→4/cc/35 blocos | 2 (12 KiB) | `final_chain` → oracles, base58, Crypto(MD5) · ← **37**; `strong_oracle_35` → final_chain, oracles, coincurve · ← 7 | **NÃO** (37 importadores por nome) — mas são a raiz da miragem: manter como *fixture* de refutação |
| **M1 – FERRAMENTA DA CADEIA-MIRAGEM** (importa `final_chain`/`strong_oracle_35`): `a1_integrity_checks.py`, `a3_rereads_attack.py`, `a4_controls.py`, `anomaly_dbbi_attack.py`, `architect_diff.py`, `blue_net_attack.py`, `chain4pw_frontier.py`, `direct_combine.py`, `duality_attack.py`, `eps35_attack.py`, `first_hint_frontier.py`, `intertwine_attack.py`/`2`/`3`/`4`, `inv2_prime_geometry.py`, `inv3_second_layer.py`, `mask_provenance.py`, `miroir_attack.py`, `mirror_attack.py`, `neighbors_attack.py`, `new_hypotheses.py`…`_6.py`, `pop_culture_attack.py`, `seed_return_attack.py`, `send_blue_sethex_attack.py`, `third_grammar_attack.py` | cadeia-miragem (premissa: cc/half/better_half/35 blocos/mask/XOR-intertwined) | Chain4pw, eps3.5, intertwined XOR, header28, Miroir p.39, neighbors… | 32 (≈370 KiB) | todos → `final_chain`, `oracles`; 25 → coincurve; `architect_diff` ← `intertwine_attack2`; `blue_net_attack` ← `send_blue_sethex_attack`; `inv2/inv3` ← `skeptic_inv23_control`; `miroir_attack`/`new_hypotheses_2-4`/`pop_culture`/`architect_diff` → `search.bifid_decrypt` | **Só em bloco** (`solver/miragem/` com `final_chain`+`strong_oracle_35`) **e editando 1 linha** de `sys.path` em cada (precisam ver `solver/` para `oracles`/`search`/`scorer`) |
| **M1 – verificadores/controles da miragem**: `skeptic_claims.py`, `skeptic_control.py`, `skeptic_inv23_control.py`, `strong_recheck.py`, `a4_controls.py`, `a1_integrity_checks.py`, `mask_provenance.py` | VERIFICADOR (controles positivos/nulos que mostram que a cadeia não ancora) | idem | 7 | → final_chain, strong_oracle_35, inv2, inv3 | idem (junto com M1) |
| **M2 – BTCSEED/BIF_REST/CANON** (sem `final_chain`): `btcseed_bazeries_attack.py`, `btcseed_duality_attack.py`, `btcseed_intertwine_attack.py`, `btcseed_payload_attack.py`, `btcseed_standard_attack.py`, `bifrest_hillclimb.py`, `bifrest_transpose.py`, `inv1_half_over_bifrest.py`, `skeptic_btcseed_null.py`, `seed_sweep.py`, `matrixsum_attack.py`, `alphabet_group_attack.py`, `faed_matrix_sum_attack.py`, `color_prime_faed_shift.py`, `colored_prime_dbbi_attack.py`, `composition_attack.py`, `vic_anchor_scan.py`, `task_1_hill_climb.py` | cadeia-miragem (premissa BTCSEED = header real) — `composition_attack`/`vic_anchor_scan`/`colored_prime_dbbi` só usam CANON como um dos alfabetos | Bifid-CANON, Bazeries, BIP32 seed, hill-climb sobre BIF_REST | 18 (≈150 KiB) | → `oracles`, `scorer`, `dsl` (5), `prime_attack` (bifrest_transpose, composition, matrixsum, five_gaps); `colored_prime_dbbi_attack` ← `anomaly_dbbi_attack` | **NÃO** individualmente (`prime_attack`/`colored_prime_dbbi` são importados); em bloco com o mesmo ajuste de `sys.path` |
| **Harness histórico Python/GPU/Ollama**: `launch.py`, `runner.py`, `gpu_search.py`, `gpu_checkerboard.py`, `gpu_trifid.py`, `interpret.py`, `selftest.py`, `vector_graph.py` | harness autônomo (llama3 local propõe, GPU escala Bifid/checkerboard/trifid) — premissa BTCSEED; `selftest.py` asserta `Bifid→BTCSEED` | Bifid genético CUDA, VIC GPU, Trifid, RAG sqlite sobre `result.json` | 8 (≈70 KiB) | `launch.py` → subprocess `gpu_search.py`/`runner.py` por `HERE`; `gpu_search` → oracles, scorer, runner(lazy), dsl(lazy), search(lazy) · ← `gpu_checkerboard`; torch/CUDA (4), Ollama :11434 (`runner`, `interpret`, `selftest`) | **NÃO** (chamadas por caminho relativo a `HERE`; `gpu_search` importado) |
| **Kit de 2.º nível na raiz** (importados por irmãos): `prime_attack.py` (←7), `checkerboard.py` (←3), `focused_aes.py` (← `kdf_variants` lazy) | KIT | REINSERTING THE PRIME BASICS; straddling checkerboard | 3 | → oracles, scorer | **NÃO** |
| **Campanhas fechadas da raiz sobre `oracles`/`scorer`** (endgame autêntico): `answer_phrase_sweep.py`, `architect_sum_attack.py`, `bip39_passphrase_attack.py`, `cosmic_book_attack.py`, `dbbi_hash_direct.py`, `dbbi_hash_preimage.py`, `dbbi_repeat_attack.py`, `first_hint_sweep.py`, `five_gaps.py`, `gsmg_sig_recover.py`, `joint_attack_v2.py`, `live_site_2026_attack.py`, `new_hints_attack.py`, `roadmap_sweep.py`, `salphaseion_passphrase_sweep.py`, `straddle_bg_attack.py`, `straddle_sweep.py`, `vic_full_attack.py`, `xor_key_attack.py` | CAMPANHA FECHADA (negativos documentados no ENDGAME) | first hint, dbbi=SHA256 64/16, straddling b/g, VIC 104,5 M, site revivido, OP_RETURN, roadmap yellowblue | 19 (≈170 KiB) | → oracles/scorer; `dbbi_repeat_attack` ← `joint_attack_v2`; `cosmic_book_attack` → `checkerboard`; `cosmic_book`/`vic_full`/`composition` gravam `solver/out/SOLVED.json` e `_work/*.jsonl` | Só os folhas sem importadores (17) são movíveis **desde que** `sys.path` continue apontando para `solver/` — na prática, ficar |
| **Campanhas fechadas da raiz sobre `gsmg_common`** (17/09 e set/2026): `exact_plaintext_tail32_attack.py`, `g_ambiguity_matrixsum_attack.py`, `lf_bookcipher_attack.py`, `lf_password_attack.py`, `lf_runningkey_attack.py`, `miroir_verbatim_attack.py`, `prime_ascii_mask_attack.py`, `prime_ascii_permutation_attack.py`, `rabbit_nest_attack.py`, `roadmap_yb_matrixsum_attack.py`, `second_door_traversals.py`, `tg2026_base9_int.py`, `tg2026_firsthint_hash.py`, `tg2026_matrix_zeros_fill.py`, `tg2026_youwon_m91.py` | CAMPANHA FECHADA | Looking Forward, Miroir verbatim, primos ASCII, ninho do coelho, 2.ª via, Telegram pós-export | 15 (≈110 KiB) | → `gsmg_common` por **caminho absoluto** (11) ou `EXPERIMENTS = ROOT/"solver"/…` (4: `g_ambiguity`, `miroir_verbatim`, `prime_ascii_*`); `miroir_verbatim` também → `final_chain`; `lf_*` → `_work/looking_forward.txt`; `rabbit_nest` → `_work/archive/Puzzle_full.png` | **SIM** para os 11 absolutos (nada os importa); os 4 com `parents[1]` assumem profundidade 1 |
| **VERIFICADORES reproduzíveis (py, endgame autêntico)**: `verify_community_claims_2026_09.py` (issues #99/#108/#110/#111), `ct_blockscan_oracle.py`, `ct_montage_attack.py`, `ct_montage_corpus_collect.py`, `tg2026_codex_hbm_null.py`, `tg2026_youwon_null.py` | VERIFICADOR | refutação de alegações da comunidade; montagem do CT (1,22 bi); nulos das cadeias Codex/YOUWON | 6 (≈50 KiB) | `verify_community_claims` → oracles, final_chain; `ct_*` → gsmg_common (abs) + `corpus.pkl` (não versionado; `ct_blockscan_oracle.MC = dirname(solver)/montagem_ct` e `premissas/multicipher_attack.CORPUS = solver/montagem_ct/corpus.pkl` apontam para o layout do scratchpad, enquanto `ct_montage_corpus_collect` grava em `solver/corpus.pkl` — **caminho inconsistente**) | `verify_community_claims`: NÃO (importa `final_chain` por nome). `ct_*`/`tg2026_*null`: SIM |
| `solver/mitm16_dbbi_hex/` (`main.go`, `make_config.py`, `go.mod`, `go.sum`) | VERIFICADOR (16! bijeções dbbi→privkey hex, MITM na curva, 0 hits) | dbbi como privkey hex | 4 (14 KiB) | `make_config.py` → `oracles` via `dirname(dirname(__file__))` (assume pai = `solver/`), coincurve; Go → dcrd/secp256k1 v4.4.1 | Diretório inteiro: **SIM** se continuar filho direto de `solver/` |
| `solver/gpu_aes16_dbbi_hex/` (`kernel.cl`, `host.py`, `refkit.py`, `validate.py`, `scan_full.py`, `launch.py`, `validate_result.json`) | VERIFICADOR/kernel pronto (gate de throughput não vencido; `lancado: false`) | dbbi-bijeção como senha AES do SMALL, OpenCL | 7 (31 KiB) | `refkit` → gsmg_common (abs) · ← `host`, `scan_full`, `validate`; `host` ← `scan_full`, `validate`; `launch` → subprocess `scan_full.py` por `HERE`; pyopencl | Diretório inteiro: **SIM** |
| `solver/prize_nonce_bsgs/` (`main.go`, `go.mod`, `go.sum`) | CAMPANHA FECHADA (BSGS sobre nonces das assinaturas do prêmio) | ECC/assinaturas | 3 (11 KiB) | Go → dcrd/secp256k1; lê `_work/prize_nonce_2026-09-17/` | **SIM** |
| `solver/primos_2026_09_17/` (`README.md`, `clean_scorer.py`, `resid_decoders.py`, `faed_primes__faed_primes.py`, `marcadores__marcadores.py`, `matrixsumlist_struct__msl_struct.py`, `critic4.py`, `critic4b.py`, `critic4c.py`, `critic4d.py`) | **LEAD VIVO** (segmentação b/be nos primos) + crítico + **scorer limpo** | yellowblueprimes | 10 (112 KiB) | todos → gsmg_common (abs); `clean_scorer` → `solver/scorer.py` (abs, subclasse) · ← `critic4`; `critic4` ← `critic4c`, `critic4d`; `critic4b` usa `WORK` absoluto e `SP = dirname(HERE)`; saídas jsonl no próprio dir (ignoradas? **não há .gitignore aqui** — cuidado ao rodar) | Diretório inteiro: **SIM** (só absolutos + `HERE`), exceto `critic4b.py` que assume pai = `solver/` |
| `solver/premissas_2026_09_17/` (`README.md`, `multicipher_attack.py`, `kfile_attack.py`, `lines_attack.py`, `infrared_spectrum.py`, `unicode_yinyang.py`, `f2_segment.py`, `f3_html.py`, `f4_passwords.py`, `retro_ebcdic.py`) | CAMPANHA FECHADA (0/148 M) + `retro_ebcdic.py` = VERIFICADOR | premissas de cifra/KDF/`-kfile`/espectro/Unicode/forense | 10 (88 KiB) | → gsmg_common (abs); `f3_html`/`f4_passwords`/`lines_attack` → `_work/gsmg_live_2026-09/`, `_work/archive`, `_work/decentraland` (abs); `multicipher` → `solver/montagem_ct/corpus.pkl` (inexistente no git); `retro_ebcdic` lê jsonl das 5 famílias no layout do scratchpad (`forense_bytes/`, `infrared/`, `kfile_first_line/`, `premissa_cifra/`, `unicode_yinyang/`) — README do dir avisa | Diretório inteiro: **SIM** |
| **Codex `.cjs` – Cifras clássicas e linguagem** (21): `ambiguous_checkerboard.cjs`, `checkerboard_matrix_keys.cjs`, `checkerboard_oracles.cjs`, `checkerboard_token_routes.cjs`, `checkerboard_token_route_oracles.cjs`, `checkerboard_word_{decoder,paths,bigrams,search,oracles}.cjs`, `joint_checkerboard_search.cjs`, `joint_checkerboard_oracles.cjs`, `run_joint_checkerboard.cjs`, `nihilist_constraints.cjs`, `nihilist_untruncated.cjs` + `verify_{ambiguous_checkerboard,checkerboard_token_routes,checkerboard_words,joint_checkerboard,nihilist_constraints,nihilist_untruncated}.cjs` | CAMPANHA FECHADA (buscas heurísticas, negativos bounded) + 6 VERIFICADORES independentes | straddling checkerboard com zero ambíguo, rotas, Nihilist | 21 | `ambiguous_checkerboard` ← `checkerboard_matrix_keys`, `joint_checkerboard_search`; `joint_checkerboard_search` ← 3; `checkerboard_word_decoder` ← 3; leem `_work/ambiguous_checkerboard_2026-09-15`, `_work/joint_checkerboard_2026-09-16`, `_work/checkerboard_exact_mask_2026-09-16`, `_work/checkerboard_token_routes_2026-09-16`, `_work/prime_geometry_2026-09-11/inputs.json` | **NÃO individualmente** (ver regra dos `.cjs` abaixo) |
| **Codex `.cjs` – Dígitos, zeros e codificações** (47): `decimal_keystream_constraints.cjs` (**hub ←45**), `zero_decimal_constraints.cjs` (**hub ←16**), `decimal_{carry_color,chunk_constraints,short_key}.cjs`, `binary_partition_endgame.cjs`, `brotli_decimal_constraints.cjs`, `columnar_decimal_constraints.cjs`, `dbbi_curve_zero_{spec,samples}.cjs`, `ebcdic_{decimal,codepoints}.cjs`, `feedback_decimal_constraints.cjs`, `fixed_zero_optional_null.cjs`, `joined_{ascii_prefix,decimal_fields}.cjs`, `morse_endgame.cjs`, `optional_null_decimal{,_oracles}.cjs`, `prime_radix_constraints.cjs`, `substituted_{codepoints,codepoint_oracles,decimal_constraints}.cjs`, `ternary_byte_packing.cjs`, `unbounded_zero_integer{,_oracles,_pilot_oracles}.cjs`, `utf8_decimal_{constraints,oracles}.cjs`, `zero_free_{numerals,plaintexts}.cjs`, `zero_symbol_constraints.cjs` + 19 `verify_*.cjs` correspondentes | CAMPANHA FECHADA (certificados de intervalo exatos) + VERIFICADORES | inteiro decimal completo com g=0/7, keystreams, EBCDIC 1141, UTF-8, Brotli, Morse | 47 | hubs acima; `_work/decimal_keystream_2026-09-15`, `_work/substituted_decimal_2026-09-15`, `_work/feedback_decimal_2026-09-15`, `_work/ebcdic_decimal_2026-09-16`, `_work/joined_fields_2026-09-16`, `_work/optional_null_decimal_2026-09-16`, `_work/utf8_decimal_2026-09-16`, `_work/zero_free_numerals_2026-09-17`, `_work/zero_symbol_2026-09-15`, `_work/shared_numeric_2026-09-11` | idem |
| **Codex `.cjs` – Matriz, cores e geometria** (41): `blue_hex_constraints.cjs`, `color_{factor_sum_passwords,residue_carry,residue_decimal,sum_any_order,sum_constraints,sum_extended,sum_symbolic}.cjs`, `count_prime_matrix.cjs`, `dbbi_matrix_hash.cjs`, `diagonal_sum_{constraints,inverse}.cjs`, `matrix_sum_{words,words_oracles,quadgrams}.cjs`, `positional_prime_{bounds,order,prefix}.cjs`, `prime_geometry_constraints.cjs`, `prime_host_{delta,faed_keys,l84_followthrough}.cjs`, `prime_product_matrix.cjs`, `prime_scale_constraints.cjs`, `ring_sum_decimal.cjs`, `rsa_color_{blocks,candidates,multibyte,oracles}.cjs` + 16 `verify_*.cjs` | CAMPANHA FECHADA + VERIFICADORES | matrizes 14×14, somas de linha/coluna/diagonal/anel, cores como pesos, primos-hospedeiro (Atlas/Doober 17/09) | 41 | `color_sum_constraints` ← 2; `rsa_color_blocks` ← 4; `prime_host_delta` ← `prime_host_l84_followthrough`; `diagonal_sum_constraints` ← `diagonal_sum_inverse`; `_work/matrix_hint_2026-09-11`, `_work/blue_hex_2026-09-11`, `_work/prime_geometry_2026-09-11`, `_work/*_2026-09-16`, `_work/prime_host_*_2026-09-17`, `_work/diagonal_sum_inverse_2026-09-17` | idem |
| **Codex `.cjs` – Primos, bases e RSA** (30): `integer_power_constraints.cjs`, `prime_alphabet_words.cjs`, `prime_codepoints.cjs`, `prime_codepoint_oracles.cjs`, `prime_joined_words.cjs`, `prime_joined_word_oracles.cjs`, `prime_radix_beam.cjs`, `prime_space_dfa.cjs`, `prime_space_words.cjs`, `radix127_completion.cjs`, `rsa_pending_{candidates,dfa,oracles}.cjs`, `rsa_substituted_{digits,oracles}.cjs`, `source_prime_radix.cjs`, `url_prime_reinsertion.cjs`, `zero_cells_prime_{stream,sums}.cjs` + 11 `verify_*.cjs` | CAMPANHA FECHADA + VERIFICADORES | radix primo, potências, gramáticas RSA de livro-texto, DFA de palavras | 30 | `prime_alphabet_words` ← 2; `prime_space_dfa` ← 2; `rsa_pending_dfa` ← 2; `rsa_substituted_digits` ← 1; `verify_rsa_pending_dfa`/`verify_rsa_substituted_digits` são `require()`d por buscadores (`prime_zero_threshold`, `unbounded_zero_codepoints`, `rsa_substituted_oracles`, `prime_codepoints`) | idem |
| **Codex `.cjs` – Fontes, formatos e auditorias** (27): `compressed_payload_constraints.cjs`, `dbbi_distance_parity.cjs`, `dbbi_gram.cjs`, `dbbi_gram_mod10.cjs`, `frequency_prime_passwords.cjs`, `wavelength_prime_passwords.cjs`, `mp3_container_audit.cjs`, `prize_nonce_recurrence.cjs`, `prize_signature_inputs.cjs`, `rabbit_reference_delta.cjs`, `recipe_consequences.cjs`, `recipe_dependency_audit.cjs`, `substituted_format_headers.cjs`, `substituted_zlib_{search,campaign}.cjs`, `title_position_masks.cjs`, `unbounded_integer_words{,_oracles,_resume,_wordcase}.cjs` + 8 `verify_*.cjs` (`verify_signature_recovery.cjs` audita `gsmg_sig_recover.py`) | CAMPANHA FECHADA / AUDITORIA + VERIFICADORES | formatos (DEFLATE/zlib/Brotli/MP3), receita de X, assinaturas do prêmio, Gram/paridade | 27 | `unbounded_integer_words` ← 5; `compressed_payload_constraints` ← `substituted_zlib_search`; leem `_work/decentraland` (MP3), `_work/recipe_audit_2026-09-11`, `_work/signature_audit_2026-09-16`, `_work/prize_nonce_2026-09-17` | idem |
| **Codex `.cjs` – Operações, fluxos e transposições** (8): `dbbi_columnar_decimal.cjs`, `xor_dbbi_followup.cjs`, `xor_knownkey_constraints.cjs`, `xor_period_constraints.cjs` + `verify_{dbbi_columnar_decimal,xor_dbbi_followup,xor_knownkeys,xor_period}.cjs` | CAMPANHA FECHADA + VERIFICADORES | XOR periódico, colunar | 8 | `_work/xor_period_2026-09-15`, `_work/dbbi_columnar_2026-09-16` | idem |
| **Codex `.cjs` sem citação por caminho em nenhum `.md`** (8): `prime_zero_threshold.cjs`, `radix127_completion_oracles.cjs`, `unbounded_zero_{codepoints,keyformats,oracles,words}.cjs`, `verify_unbounded_zero_codepoints.cjs`, `zero_decimal_constraints.cjs` | CAMPANHA FECHADA (citados só por nome-base nos relatórios; `zero_decimal_constraints` é hub ←16) | zeros/primos | 8 | — | idem |

**Regra dos `.cjs` (182 = 125 buscas/oráculos + 57 `verify_*`, 1.325 KiB):** só é seguro mover **todos juntos** para outro diretório **de profundidade 1** (ex.: `codex/` na raiz do repo, nunca `solver/codex/`), porque `path.resolve(__dirname,'..')` precisa continuar sendo a raiz do repo e os `require('./x.cjs')` precisam dos irmãos. Mesmo assim ficam obsoletas 169 entradas em `docs/research-manifest.json` (HEAD), os exemplos `node solver/<x>.cjs` em 69 `_work/**/RELATORIO.md` e o `solver/README.md`. Recomendação: **ficar**.

---

### (b) LOG/SAÍDA e OBSOLETOS rastreados em `solver/` — apagáveis sem perder reprodutibilidade

| Arquivo | Tipo | Bytes | Motivo |
|---|---|---|---|
| `solver/gpu_aes16_dbbi_hex/validate_result.json` | LOG/SAÍDA (único no `solver/`) | 1.311 | resultado da validação do kernel (controles A/B ok, 44–51 M/s, `lancado: false`). É evidência do gate; **preferível mover para `_work/`** a apagar |
| `solver/experiments/claude_endgame_2026_09_02/patch_round2.py` | OBSOLETO (quebrado) | 9.364 | edita in-place um `.js` de workflow em `C:\Users\ruthe\.claude\projects\...\75ecc5a7...\workflows\scripts\` que não existe no repo |
| `solver/experiments/claude_endgame_2026_09_02/patch3x3.py` | OBSOLETO (patcher já aplicado) | 6.500 | reescreve `bifid3x3_exhaustive.py` via `str.replace`; re-rodar corrompe o script |
| `solver/experiments/claude_endgame_2026_09_02/patch_dgs.py` | OBSOLETO (patcher já aplicado) | 4.667 | idem sobre `dutch_german_scorer.py` |
| `solver/experiments/claude_endgame_2026_09_02/bench3.py` | OBSOLETO (benchmark) | 1.150 | `exec()` de metade do fonte de `bifid3x3_exhaustive.py` só para medir tempo |
| `solver/experiments/claude_endgame_2026_09_02/bench_hc.py` | OBSOLETO (benchmark) | 617 | precisa de `quad_nl.pkl` não versionado |
| `solver/experiments/claude_endgame_2026_09_02/bs_summary.py` | OBSOLETO | 480 | lê `blob_structure_result.json` (cwd relativo, não versionado) |
| `solver/experiments/claude_endgame_2026_09_02/dbg_cb.py` | OBSOLETO (debug) | 1.147 | rascunho de checkerboard com README absoluto |
| `solver/experiments/claude_endgame_2026_09_02/fetch_corpus.py` | OBSOLETO (downloader) | 1.377 | baixa Wikipedia nl/de para `corpus/` (ignorado); sem docstring nem relatório |
| `solver/experiments/claude_endgame_2026_09_02/ps_sanity.py` | OBSOLETO (sanity) | 1.299 | asserts sobre `permutation_search.py` |
| `solver/experiments/claude_endgame_2026_09_02/tg_mine.py` | OBSOLETO (grep do Telegram) | 2.208 | regexes sobre `result.json`, grava `cat_*.txt`; superado por `telegram_miner.py` |
| `solver/experiments/claude_endgame_2026_09_02/lf_runkey.py` | OBSOLETO (duplicata) | 6.987 | **byte-idêntico** a `solver/lf_runningkey_attack.py` (`diff` = 0 linhas); a cópia da raiz é a citada no ENDGAME (commit `9ff8497`) |
| `solver/task_1_hill_climb.py` | OBSOLETO (stub) | 760 | 20 linhas, premissa BIF_REST/BTCSEED, não citado em nenhum doc; coberto por `bifrest_hillclimb.py` |
| `solver/lf_password_attack.py` | **QUEBRADO, mas não apagar** | 5.052 | lê `...\Temp\claude\...\e6b07645...\scratchpad\lf.txt` (linha 49); resultado está documentado — precisa de correção de caminho para `_work/looking_forward.txt`, não de remoção |

Total apagável limpo: 13 arquivos, ≈37 KB. Também candidatos a **remoção em bloco** (não quebrados, mas premissa refutada e dependentes de torch/Ollama): o harness histórico (`launch.py`, `runner.py`, `interpret.py`, `selftest.py`, `gpu_search.py`, `gpu_checkerboard.py`, `gpu_trifid.py`, ≈70 KiB) — só se `gpu_checkerboard.py` sair junto (importa `gpu_search`) e `solver/README.md`/`SKILL.md` forem atualizados.

---

### (c) NÃO apagar nem mover (kit e dependentes por caminho)

- **Núcleo:** `solver/oracles.py`, `solver/scorer.py`, `solver/dsl.py`, `solver/search.py`, `solver/experiments/claude_endgame_2026_09_02/gsmg_common.py` (e, por consequência, o diretório `solver/experiments/claude_endgame_2026_09_02/` inteiro — 45 caminhos absolutos externos + `SP = dirname(__file__)` nos 77 irmãos).
- **Fixtures da cadeia refutada, importadas por nome:** `solver/final_chain.py` (←37), `solver/strong_oracle_35.py` (←7).
- **Módulos da raiz importados por irmãos:** `prime_attack.py` (←7), `checkerboard.py` (←3), `blue_net_attack.py` (← `send_blue_sethex_attack`), `architect_diff.py` (← `intertwine_attack2`), `colored_prime_dbbi_attack.py` (← `anomaly_dbbi_attack`), `dbbi_repeat_attack.py` (← `joint_attack_v2`), `gpu_search.py` (← `gpu_checkerboard`), `runner.py` (← `gpu_search` lazy; chamado por `launch.py`), `focused_aes.py` (← `kdf_variants` lazy), `inv2_prime_geometry.py` e `inv3_second_layer.py` (← `skeptic_inv23_control`), `launch.py` (chama `gpu_search.py`/`runner.py` por `HERE`).
- **Kits internos do snapshot:** `permutation_search.py` (←4), `permutation_search2.py`, `brainwallet.py`, `select_bits.py`, `seed_encoding_in_bif.py`, `yinyang_interleave_pass1.py`, `bifid3x3_exhaustive.py` (exec por `bench3`), `dutch_german_scorer.py` (alvo de `patch_dgs`).
- **Subdiretórios:** `solver/primos_2026_09_17/clean_scorer.py` (scorer limpo; ← `critic4`) e `critic4.py` (← `critic4c`, `critic4d`); `solver/gpu_aes16_dbbi_hex/refkit.py`, `host.py`, `scan_full.py`; `solver/mitm16_dbbi_hex/make_config.py` (assume pai = `solver/`).
- **Hubs `.cjs`:** `decimal_keystream_constraints.cjs` (←45), `zero_decimal_constraints.cjs` (←16), `decimal_carry_color.cjs` (←5), `unbounded_integer_words.cjs` (←5), `rsa_color_blocks.cjs` (←4), `zero_free_numerals.cjs` (←4), `utf8_decimal_constraints.cjs`, `verify_utf8_decimal.cjs`, `joint_checkerboard_search.cjs`, `checkerboard_word_decoder.cjs` (←3 cada) e os demais 37 `.cjs` `require()`d ao menos uma vez (lista completa no grep acima) — na prática, **os 182 `.cjs` como bloco**.
- **Verificadores citados por caminho em docs:** `verify_community_claims_2026_09.py`, `ct_blockscan_oracle.py`, `blue_net_attack.py`, `send_blue_sethex_attack.py`, `gsmg_sig_recover.py`, os 57 `verify_*.cjs`.

---

### (d) Grafo de imports do kit

```
README.md ("> d b b i…", blobs)  result.json (Telegram, 27 MB, não versionado)
        │                                │
        ▼                                ▼
solver/oracles.py  ◄──82            solver/scorer.py  ◄──33   (grava solver/quadgram.pkl, ignorado)
   sources() blobs() aes_open()          Scorer() build()
   check_privkey() check_mnemonic()           ▲
        ▲        ▲          ▲                 │ subclasse
        │        │          │      solver/primos_2026_09_17/clean_scorer.py ◄── critic4.py ◄── critic4c.py, critic4d.py
        │        │          │
        │        │   solver/dsl.py ◄──14 (runner, search, selftest, bifrest_hillclimb, btcseed_*×4, inv1,
        │        │     _bifid CANON      skeptic_btcseed_null, skeptic_claims, task_1, quick_battery, gpu_search lazy)
        │        │          ▲
        │        │   solver/search.py (→dsl, oracles; scorer lazy) ◄──7 (architect_diff, miroir_attack,
        │        │                                                    new_hypotheses_2/3/4, pop_culture_attack, gpu_search lazy)
        │        │
        │   solver/final_chain.py (→oracles, base58, Crypto.MD5) ◄──37 [M1: cadeia-miragem]
        │        ▲
        │   solver/strong_oracle_35.py (→final_chain, oracles, coincurve) ◄──7 (a3, a4, inv2, inv3, skeptic_control,
        │                                                                     skeptic_inv23_control, strong_recheck)
        │
        │   solver/prime_attack.py (→oracles, scorer) ◄──7   solver/checkerboard.py (→oracles, scorer) ◄──3
        │   solver/blue_net_attack.py ◄── send_blue_sethex   solver/architect_diff.py ◄── intertwine_attack2
        │   solver/gpu_search.py (→oracles, scorer; runner/dsl/search lazy) ◄── gpu_checkerboard ◄── launch.py (subprocess)
        │
        └── SOLVER = r"C:\...\solver" (absoluto, linha 9)
solver/experiments/claude_endgame_2026_09_02/gsmg_common.py (→oracles; scorer lazy; Crypto, coincurve, base58) ◄──104
   ├─ 77 .py do snapshot (SP = dirname(__file__) ou absoluto)
   │    permutation_search ◄── permutation_search2 ◄── ps2_null; ps_sanity; brainwallet ◄── brainwallet2;
   │    select_bits ◄── sb_control2; seed_encoding_in_bif ◄── _addendum; yinyang_interleave_pass1 ◄── yinyang_interleave;
   │    kdf_variants → solver/focused_aes (lazy); quick_battery/tail32_history/seed_encoding_in_bif → oracles direto
   ├─ 20 .py da raiz (11 absolutos; 4 via ROOT/"solver"/experiments/…; ct_* via KIT absoluto)
   ├─ solver/premissas_2026_09_17/*.py (9, absoluto/KIT)
   ├─ solver/primos_2026_09_17/*.py (9, absoluto/KIT)
   └─ solver/gpu_aes16_dbbi_hex/refkit.py (absoluto) ◄── host.py ◄── scan_full.py, validate.py; launch.py ─subprocess─► scan_full.py
solver/mitm16_dbbi_hex/make_config.py → oracles (dirname(dirname(__file__)) = solver/) → config.json → main.go (Go, dcrd/secp256k1)
```

Bibliotecas de terceiros por dependência: `Crypto` (63 scripts), `coincurve` (61), `numpy` (35), `base58` (19), `mnemonic` (10), `bip_utils` (9), `ecdsa` (4), `torch` (4: harness GPU), `PIL` (3), `scipy`/`cv2`/`pyopencl` (1 cada); Ollama `:11434` em `runner.py`, `interpret.py`, `selftest.py`; Node 24 nativo nos 182 `.cjs`; Go 1.26 + dcrd/secp256k1 v4.4.1 nos dois `main.go`.