# BRIEFING — endgame GSMG.IO 5 BTC (SalPhaseIon / Cosmic Duality)

Você é um agente de criptoanálise. Objetivo: encontrar a chave privada do endereço-prêmio
`1GSMG1JC9wtdSwfwApgj2xcmJPAwx7prBe` (pubkey on-chain `04f4d1bb…bf33559`). **Só um oráculo
duro conta como solução**: (a) privkey → endereço-prêmio; (b) um blob AES abrir com plaintext
SEMÂNTICO (≥85% ASCII imprimível, ou contendo WIF/hex64/BIP39). Padding PKCS7 válido sozinho é
ruído (1/256). Legibilidade (quadgramas) é só triagem.

## Ambiente
- Python: `C:/Users/ruthe/AppData/Local/Programs/Python/Python312/python.exe` (tem pycryptodome, ecdsa, base58, mnemonic, bip_utils, coincurve, PIL).
- Kit: `import sys; sys.path.insert(0, r"C:\Users\ruthe\Desktop\puzzle\gsmgio-5btc-puzzle\solver\experiments\claude_endgame_2026_09_02"); import gsmg_common as G`
  (rode `python gsmg_common.py` uma vez: self-test reproduz BTCSEED, o z-method, a espiral, o checkerboard da fase 3.2.2).
- Escreva seu script em `<scratchpad>/<sua_familia>.py`, log JSONL em `<scratchpad>/<sua_familia>.jsonl`. Repo (leitura): `C:\Users\ruthe\Desktop\puzzle\gsmgio-5btc-puzzle` (README.md = todas as fases; ENDGAME.md = 2000 linhas de negativos; `solver/` = scripts antigos; `result.json` = export Telegram 27 MB, só grep).
- API do kit: `G.DBBI, G.FAED, G.TOKENS, G.BLOBS{SMALL,COSMIC,TAIL32}, G.MATRIX_IMG/MATRIX_README, G.SPIRAL, G.COLORED, G.BLUE_IDX, G.YELLOW_IDX, G.row_sums/col_sums, G.digits(s), G.z_method(digs), G.aes_try(pw, blob) -> [(kdf, plaintext)], G.try_password_all(pw) -> (hard, soft), G.aes_rawkey(key32, blob, iv), G.semantic(p), G.priv_hit(b32), G.scan_priv(buf), G.phrase_priv(phrase), G.bifid(text, alphabet, period, n, mode), G.CANON, G.keyed_alphabet(phrase), G.bif_full(), G.checkerboard_decode(digs, alphabet, escapes, universe), G.english_score(t), G.word_hits(t), G.jsonl(path, obj)`.

## A página (verbatim, byte-verificada contra o site vivo) — ordem de leitura
1. `dbbi` — 91 símbolos a–i: `dbbibfbhccbegbihabebeihbeggegebebbgehhebhhfbabfdhbeffcdbbfcccgbfbeeggecbedcibfbffgigbeeeabe`
2. binário a/b (a=0,b=1, 8 bits) → **`matrixsumlist`**
3. `faed` — 570 símbolos a–i (15×38 = 570 = 2×285): começa `faedggeedfcbdabhhggcadcfeddgfdgbgigaaedggiafaecghggcd…` (G.FAED)
4. `z` + segmento a–i+o (a=1..i=9, o=0 → decimal → base16 → ASCII) → **`lastwordsbeforearchichoice`**
5. `z` + segmento → **`thispassword`**
6. `z` + `shabef` (a1z26 = "sha256") + `our first hint is your last command`
7. blob AES **SMALL** (openssl `Salted__`, 80 B de CT = 5 blocos) — a 2ª linha base64 é precedida pelo binário **`enter`** (a/b), i.e. o "enter" marca a quebra de linha do base64.
8. `shabef ans too` (= "sha256 answer too")
9. Textarea 2 "Cosmic Duality": blob AES **COSMIC** (1328 B de CT).
Também existe o blob **TAIL32** no fim da fase 3.2 (após "Raising the stakes without extra chances of winning. A fubcd-king & oracle-queen, thingky mvps, on a sad board but as wide as the first one seen."), 80 B de CT, nunca aberto.
Página tem só dois `<h1>`: `SalPhaseIon` e `Cosmic Duality`; `<title>GSMG Puzzle</title>`. Nada mais.
SalPhaseIon = "Salvation" com "vat"→"Phase" (Architect: "the door to your right leads to the source and the salvation of Zion"). Criador: "Has anyone passed the salvation part?" → "Partly" (2023).

## Fatos verificados que MUDAM o mapa (leia com atenção)
- **Todos os "decrypts" da cadeia comunitária (Chain1 79 B, Chain2 79 B, Cosmic 1327 B, Chain4 1151 B) têm padding 0x01** — a assinatura exata de um falso-positivo de padding (uma mensagem real teria pad=1 só 1/16 das vezes; 4 seguidas = 1/65536). Os plaintexts são alta-entropia. A senha do Cosmic `a795de11…` foi CONSTRUÍDA como XOR de sha256 de tokens (a origem é a construção; abrir com pad válido é 1/256 por tentativa entre milhares). **Trate Chain1→4, `cc`, half/better_half, matriz 103² e "35 blocos" como MIRAGEM.** Nenhum blob foi aberto de verdade. A fronteira real é: decodificar dbbi/faed → senha(s) → abrir SMALL/COSMIC/TAIL32.
- **Matriz 14×14 original tem 102 uns** (célula (7,6) preta = índice espiral **193, primo**); README zerou para 101 (primo). Cauda central espiral `0100`. Quase todo trabalho anterior usou 101. Somas de linha (imagem): `[6,10,8,7,6,6,5,5,9,9,7,8,7,9]`, colunas `[8,10,8,10,8,7,4,6,7,5,9,6,6,8]`. Pixel `#FEFEFE` em (7,4) = espiral 163 (primo). 24 células coloridas nos índices espirais 7,15,…,191 (LSB de cada byte da URL; azul=1 amarelo=0): 15 azuis, 9 amarelas; azul `#3F48CC`, amarelo `#FFF200`. 24 = quantidade de primos < 91 = len(dbbi).
- **BTCSEED** (foothold único e real): `G.bifid(G.FAED, G.CANON, 570)` começa com `BTCSEED`; CANON = ordem de 1ª ocorrência do dbbi (`d,b,i,f,h,c,e,g,a`) + filler alfabético (I=J). O header depende SÓ de `faed[0:4]="faed"` e `faed[285:289]="aied"` (Bifid-570 pareia símbolo i com i+285: "half and better half"). Os 563 chars seguintes são ilegíveis (score −5,58; inglês real ≈ −3). Posições pares da saída ∈ {B,C,D,E} (artefato estrutural). Significância real ≈ 1e-4 após contar a busca de períodos; **pode ser coincidência** — não construa castelos sobre ele.
- Gramática provada nas fases 1–3: sha256(concatenação de palavras-chave, caixa preservada, sem espaços) em hex = senha `openssl enc -aes-256-cbc -d -a -pass pass:<hex>` (KDF EVP-MD5 no openssl da época; teste MD5 e SHA256). Fase 3.2: Beaufort (chave THEMATRIXHASYOU), VIC straddling checkerboard com alfabeto de frase (`FUBCDORA.LETHINGKYMVPS.JQZXW`, escapes 1,4), EBCDIC cp1141, a1z26. Página endgame: a-i→dígitos→decimal→hex→ASCII; a/b→binário→ASCII.
- HASHTHETEXT: sha256(`GSMGIO5BTCPUZZLECHALLENGE1GSMG1JC9wtdSwfwApgj2xcmJPAwx7prBe`) = `89727c…` = URL do endgame ("o autor é picky com o que considera texto": letras/dígitos, sem pontuação/espaços, caixa preservada).

## Hints primários do criador (Jrk Bgrt / @SoWut)
- Roadmap (2023-02-25): `yellowblueprimes → matrixsumlist → lastwordsbeforearchichoice → yinyang`; "we wont give away the password its in front of your eyes but you're not seeing it"; "very last step is a true give away promised". 2026-03-03: "it's in front of your eyes but you're not seeing it" → **"Bingo"**.
- "Roses are White but often Red. Yellow has a number and so does Blue. Go back to the first puzzle piece without further ado. It might have shown you only one door, beware that the rabbits nest may contain a whole lot more. Hush hush." (levou ao HASHTHETEXT → URL do endgame; talvez também descreva o passo `yellowblueprimes`).
- "At least a prime number is very important to get any further" (2023); "Some characters need to be 'zeroed out'" + "prime positions" (2021); "First or zero" (índice base 0 ou 1); "R=18 / A=1 / B=2. Could also be 21 or 1812 bit" (2021-04-01); "Regular Bitcoin Private key" (2024 — alvo é privkey crua de 32 B, não seed BIP39); "Have you tried the purple pill already?"; "Cosmic Duality Book Page — Life and Death" (Time-Life *Cosmic Duality*, p.39 gravura "Le Miroir de la Vie et de la Mort"); "rewatch episode 3.5 with the better half" (Mr. Robot eps3.5_kill-process.inc); "Once you hit a ying yang, you'll be able to solve it the same day"; Decentraland: HASHTHETEXT; "GSMG = Globally supporting my generation"; passaporte do Neo 11 SEP 2001 = "a única data".
- Texto do Arquiteto (fase 3.2.1): "…RETURN TO THE SOURCE CODES … REINSERTING THE PRIME BASICS AFTER WHICH YOU WILL BE REQUIRED TO SELECT FROM OVER TWENTY-THREE CIPHERS SIXTEEN ENCRYPTIONS AND OR SEVEN INTERTWINED PASSWORDS TO FIND THE ACTUAL PRIVATE KEYNOTE THAT ALSO BRUTE FORCING MIGHT BE REQUIRED … WORTH HUNDRED FOURTY …". Fase 3.2.2 (VIC): "IN CASE YOU MANAGE TO CRACK THIS THE PRIVATE KEYS BELONG TO HALF AND BETTER HALF AND THEY ALSO NEED FUNDS TO LIVE". Fase 1: "the seed is planted"; fase 2 senha "theflowerblossomsthroughwhatseemstobeaconcretesurface"; fase 3: causality/Safenet/Luna/HSM/11110/genesis-hex/FEN; 3.2: jacquefresco·giveit·justonesecond·heisenbergsuncertaintyprinciple.

## Famílias JÁ FECHADAS com oráculo (NÃO repita; cite se precisar)
Bifid canônico em todos os períodos/direções/canais par-ímpar; hill-climb do filler e de substituição mono sobre a saída; VIC/checkerboard em faed e dbbi com 4 alfabetos × 9! mapeamentos × 36 escapes (104 M decodes) e com hill-climb GPU de quadrados; Trifid; Bazeries; transposição colunar por matrixsumlist/lastwords…/dbbi (pré e pós-Bifid); matrixsumlist (=101, somas README) como keystream mod-9/índices/período; dbbi como keystream/Vigenère/Beaufort; extração direta de chave (BIP39/WIF/base26/base9/hex) de faed/BIF/canais; ~230k senhas de tokens/frases do Arquiteto/roadmap/Matrix/Mr.Robot/Venus Project em SMALL/COSMIC (raw/sha256/upper); sha256(frase)→privkey para ~200 frases; color-prime (17/41, permutação 256134789, zerar primos coloridos no dbbi como fonte de keyword); BIP39 dos índices coloridos; matriz 14×14 como entropia BIP39; XOR-de-sha256 de subconjuntos de tokens (~1.900 seleções) contra os "35 blocos"; SEND THE BLUE TO SET HEX; estego em PNG (só cores/chunks — **canal alfa e planos LSB NÃO foram checados**); tudo sobre os 35 blocos/cc/half/better_half (miragem).

## Regras
1. Escreva a hipótese em prosa (1–3 frases, falsificável, espaço finito) ANTES de codar; registre-a no JSONL.
2. Só oráculo duro declara solve. Reporte contagens exatas (n_tests) e o melhor "soft" (padding válido / score) honestamente.
3. Rode um CONTROLE positivo do seu decoder quando fizer sentido (ex.: checkerboard reproduz a fase 3.2.2; Bifid reproduz BTCSEED).
4. Não sweep cego gigante: cada família ≤ ~1–5 milhões de operações baratas; priorize leituras principiadas.
5. Retorno final = JSON estrito (o orquestrador valida): `{"family","hypothesis","n_tests","hard_hits":[...],"soft_hits":[...],"best_readable":{"score","text","how"},"verdict","new_leads":[...],"script"}`. Se `hard_hits` não for vazio, inclua TUDO para reproduzir (senha/chave exata, blob, kdf, plaintext completo em hex).

## ADENDO — resultados da RODADA 1 (2026-09-02, 18 famílias, ~5,6 M testes, 0 hits)
**Fatos novos verificados:**
- **KDF do criador = EVP_BytesToKey-SHA256** (openssl ≥ 1.1.0): os blobs das fases 2, 3 e 3.2 abrem SÓ com SHA256, nunca com MD5. Use `G.aes_try(pw, blob, kdf="sha256")` como padrão (MD5 opcional). Corolário: a "cadeia" comunitária (Chain1/Cosmic `a795de11…`) usava EVP-MD5 → mais uma evidência de miragem.
- **TAIL32 é real e nunca foi aberto** — criador (2023-06-10): "Correct" para "o bloco final da fase 3.2 não foi decifrado"; "No" para "é o hint da SalPhaseIon". `oracles.aes_open` histórico só tinha SMALL/COSMIC → os ~230k senhas antigas NUNCA foram testadas contra TAIL32.
- **Estatística refuta Bifid-570 real e checkerboard direto sobre faed**: MI no lag 285 z=−0,2 (Bifid-570 de texto estruturado exigiria dependência); H_cond z=−0,75 (checkerboard puro daria ≈−11). faed comporta-se como stream i.i.d. com forma unigrama de checkerboard (g,i ≈32% como "escapes"). **Trate BTCSEED como provável coincidência.** dbbi (n=91) tem forma de checkerboard curto (b,e como escapes) mas pouca potência.
- **PNGs limpos em definitivo** (alfa, LSB, sub-pixel, chunks, IDAT, QR, Wayback). Não gastar mais.
- Matriz 102: os índices espirais 0..191 têm exatamente **91 zeros** (= len(dbbi)); desses, 15 estão em índices primos e 9 são as amarelas.
- Criador (2021-03-05): **"Infrared"** em resposta a "xor black/white/yellow/blue? or sum for red". Criador (2023-01-25): "42" como quantidade de matemática (H=−42 na tabela da fase 2 `# X 2 S H 4 Y 0 Q B 15 #`, com S=32, B=−16, Q/X/Y desconhecidos; criador: "can't say anything about this" — NÃO descartado).
- Comentário HTML na página da fase 2/3 (2024): `<!-- You made it to the next step! Good luck little bunny hunter 😉 -->` (testado como senha: negativo).
- `<h1> SalPhaseIon </H1>` fecha em maiúsculas; `<h1> Cosmic Duality </h1>` em minúsculas (única assimetria da página).

**Famílias FECHADAS na rodada 1 (não repetir):** hashthetext (4.372 textos × 6 formas × 3 blobs × 2 KDF + -K com 5 IVs); brainwallet (3.246 frases × 10 formas + espelho); halfhalf_arith (op mod 9/10 das metades); checkerboard com 60 alfabetos de frase × 72 escapes × identidade; Bifid 5×5 com 120 alfabetos keyed × todos os períodos; Bifid 3×3 com 6 quadrados; Polybius 9×9/27/T9/a1z26; matriz verdadeira (102) como keystream/transposição/senha; geometrias do dbbi (27 listas de somas); zeroed-primes/z-method em dbbi/faed; blob_structure (linha1/linha2, 13 modos legados, TAIL32 com textos da 3.2 diretos); keystream de tokens/cores/primos (99 keystreams × 7 modos, pré e pós Bifid); leituras geométricas naturais do faed (765) e dbbi (136); seed codificada no BIF (hex-map, base-25/26, prefixos BIP39); mineração Telegram (81 candidatos).

**Kit atualizado:** `G.wif_candidates/hex64_candidates` (exigem dígito+caixa mista), `G.semantic_text(t)` para saídas textuais (quadgramas/palavras), `G.fast_priv_scan(buf)` (coincurve), `G.PHASE2_B64` (controle de KDF). NÃO use `G.semantic` em saídas de decoders que só emitem bytes 32..126 (tautologia).

## ADENDO — resultados da RODADA 2 (2026-09-02, 8/13 famílias concluídas, ~8,5 M testes, 0 hits)
**CORREÇÃO do mapa — a "matriz 102" é ARTEFATO.** As células (6,6),(6,7),(7,6),(7,7),(7,8),(7,9),(8,6) da imagem original NÃO são uniformes: o centro contém o desenho de um **coelho branco** (orelhas, olho, corpo, cauda) em resolução 5 px, atravessando a grade de 25 px. Fração de pixels pretos medida por célula: (6,6)=0,16 (6,7)=0,36 (7,6)=0,24 (7,7)=0,12 (7,8)=0,36 (7,9)=0,16 (8,6)=0,08 — todas as demais 189 células são exatamente 0,00 ou 1,00. Logo `G.MATRIX_IMG[7][6]=1` ("102 uns", "índice 193 primo", "cauda 0100", "91 zeros em 0..191") é amostragem errada de um desenho; a matriz de BITS real é a do README (**101 uns, `G.MATRIX_README`**) + um pictograma central que não é dado de célula. O bitmap 5 px do pictograma (bbox sub-linhas 30–44 × sub-colunas 32–49 = 270 bits, em `matrix_prime_bits.jsonl` campo `rabbit_bits`) foi testado row/col-major como bits/bytes/decimal/sha256/privkey: negativo. Provável decoração ("follow the white rabbit" / "in front of your eyes").

**Famílias FECHADAS na rodada 2 (não repetir):**
- `tail32_history` (3,48 M): 466.310 senhas-base (308k históricas RE-GERADAS de todos os scripts de solver/ + 158k da gramática da fase 3.2) × 3 formas × TAIL32 × EVP-SHA256 e MD5 + 933k privkeys sha256(senha). Paddings válidos = exatamente 1/256. Se TAIL32 usa sha256(concatenação), as palavras NÃO estão no vocabulário comunitário nem nos tokens da 3.2.
- `dbbi_in_zeros` (45k): dbbi nas 91 células-zero (todas as ordens), extrações por primos/amarelas/163, somas da matriz preenchida, "zeroed out" com primos → z-method.
- `matrix_prime_bits` (95k): zerar/flipar/setar bits em índices primos × {102,101} × base × 4 ordens; bits só-primos/não-primos; canais R/G/B/IR/XOR/SUM/GRAY da imagem em espiral/row-major → mod 9/26/bytes/somas; pictograma central. Leitura literal de "primes/zeroed/infrared" sobre o bitmap está refutada.
- `kdf_variants` (1,21 M): PBKDF2-HMAC-SHA256 iter 1k/2048/10k/100k, SHA1/SHA512 10k; EVP com md5/sha1/224/256/384/512/ripemd160 (com/sem \n); -K = sha256/sha256d/utf16/hex64 × 9 IVs (zeros, salt‖salt, salt‖0, sha[16:], sha[:16], invertido, md5, CBC deslocado); aes-128/192. **Argumento estrutural: o cabeçalho `Salted__`+salt8 dos 3 blobs prova uso de -pass/-k (openssl -K não escreve cabeçalho) → a derivação é EVP-SHA256 aes-256 como nas fases 2/3/3.2; o desconhecido é a SENHA, não o KDF. Não gastar mais em KDF.** Dica: com -K, o padding do último bloco só depende de chave+CT — 1 IV basta para oráculo de padding.
- `ans_too_ct_text` (64k): base64/CT/hex/salt dos 3 blobs (1 linha, \n, \r\n, com "enter", com espaços da textarea) × 6 formas, cruzado entre blobs; sufixos de shell " ", "\n", "\r\n", BOM, UTF-16-LE em tokens e digests. sha256hex+"\n" fechado.
- `checkerboard_keystream` (1,31 M): 41 keystreams brutos × rotações × fwd/rev × add/sub/Beaufort/autokey × 3 configs = 18.426 streams; filtro invariante H_cond/token-IoC vs embaralhados (controle: chave certa z=−31; rotação errada ainda z≈−5,5); |z| máx real 5,57 = cauda do nulo; 243 aprovados → 1,29 M decodes (62 alfabetos × 72/90 escapes): melhor quadgrama −6,02. **Camada aditiva com chave variada (≥3 resíduos) refutada**; o unigrama enviesado sem memória serial é compatível com TRANSPOSIÇÃO pós-checkerboard ou com chave de POUCOS resíduos.
- `permutation_search` (1,88 M): fitness MI lag-1/H_cond/IoC vs nulo (controle recupera a permutação inversa exata do checkerboard 3.2.2 embaralhado); 3.381 permutações estruturadas de faed (primos/coloridos/mod k/16 grades × 24 leituras/metades/colunar keyed 50 chaves × larguras 5..40, com inversas) → max |z| 3,67 = máximo esperado de 3.381 normais; dbbi 2.900 → 3,23. Hill-climb estruturado NÃO discrimina (z=76,8 em faed vs 75,3 em faed embaralhado). **faed não é transposição (nessas famílias) de stream serial; checkerboard/VIC direto E permutado excluídos (z=0,6 com poder 4× o do controle).** Único resíduo: IoC de dígrafos na identidade faed z=1,68, dbbi z=2,03 (fracos, positivos).
- `phase2_leftovers` (420k): tabela `# X 2 S H 4 Y 0 Q B 15 #` = [X,2,32,−42,4,Y,0,Q,−16,15] em 2.304 sequências × 5 formas na gramática da fase 3 (prefixo/sufixo/8ª parte, FEN pré-lance); 90 palavras da fase 2 × 9 formas; 80 alfabetos keyed da fase 2 em checkerboard (72 escapes, a=0/1) e Bifid 5×5 (períodos 1..570); 1.920 keystreams dos números. Q proposto: phishing/phish; Twofish→2 (Threefish→3); 82=ASCII 'R' (worst gear = Reverse). "the I and W are below" sem leitura firme.

**Leads principiados que sobraram (ainda não testados por ninguém):**
1. faed como fonte de entropia i.i.d. (570 dígitos base-9 ≈ 1.806 bits) de onde 32 bytes/256 bits são **SELECIONADOS** por índices (primos, coloridos, posições de g/i, múltiplos, 1ª/última ocorrência) — seleção ≠ permutação, não coberta.
2. IoC de dígrafos positivo fraco (faed 1,68 / dbbi 2,03): testar autocorrelação de n-gramas em lags curtos / repetição de blocos antes de descartar.
3. Camada aditiva com POUCOS resíduos: binário a/b de `enter`/`matrixsumlist` (0/1), 0/±1, chaves de 2 símbolos — preservam o viés unigrama observado.
4. Tabela da fase 2 como bytes/nibbles (0x02 0x20 0x2A 0x04 0x00 0x10 0x0F) / salt / IV; cifra de teclado "I and W are below" (linha abaixo no QWERTY: I→K, W→S) sobre X,S,H,Y,Q,B; alfabetos keyed em variante fill+kw / dot-style; leitura da tabela como larguras de transposição (15 colunas; 570 = 15×38).
5. pop_culture_exact ainda não rodou (bloqueio de filtro de conteúdo ao gerar falas de filme): refazer SEM transcrições — só textos do próprio puzzle e nomes/títulos curtos.
- `insert_zeros_primes` (inline, 1.836 testes, 0): "zeroed out"/"prime positions"/"First or zero" lidos como INSERÇÃO de '0' nas posições primas (ou não-primas), base 0/1, em dbbi/faed/metades/concatenações/reversos, seguido do z-method da página (decimal→hex→bytes): nenhum printable ≥ 0,85, nenhuma privkey, nenhuma senha (dec/hex/bytes/sha256) abre os 3 blobs.
- `unigram_model` (inline): o unigrama de faed é INCOMPATÍVEL com dígitos uniformes em base 9 (χ²=43,7, df 8) e com mapa hex 16→9 (χ²≈41), mas COMPATÍVEL com dígitos DECIMAIS uniformes onde g representa dois dígitos (0 e 7): χ²=11,5 (p≈0,17); faedA g-dobrado χ²=9,2; faedB pior (i-dobrado 17,6); dbbi não ajusta (b-dobrado 20,5). Metades: χ² A|B = 17,4 (p_perm 0,021 no corte a priori 285; melhor corte k=220 com p=0,057 após corrigir a busca) — não-estacionariedade fraca. Busca de faed/dbbi/metades/reversos com coringa {0,d} em cada letra e com zeros removidos nos 100k primeiros dígitos de π, e, √2, √3, √5, √7, √10, φ, ln2, ln10, π², e^π, ∛2 (prefixos de 14–18 dígitos): NADA.
- `g_delimiter` (inline, 360 leituras × nulo de 30 embaralhados): g (107 ocorrências, única contagem prima, z≈+5,7) e cada outro símbolo como DELIMITADOR — comprimentos dos campos (a1z26, +1, mod 9), campos como números octais nos 8 símbolos restantes (2 ordens → a1z26/mod 27), soma dos dígitos do campo → letra; em faed/dbbi/metades. Melhor z=3,8 em 360 (cauda do nulo), melhores scores −5,3..−6,9 (inglês ≈ −3). Complementa `sep_octal` (rodada 1: fluxo 3 bits e grupos octais → bytes/privkey). Negativo.
- `first_hint_rabbit` (inline, 896 testes, 0): "our first hint" = imagem "follow the white rabbit" (o coelho está desenhado no centro da matriz): 52 frases (follow the white rabbit em 8 grafias, white rabbit, rabbits nest, bunny hunter, wake up neo, the matrix has you, the seed is planted, hashthetext, URL 89727c…, hush hush, purple pill, infrared, bingo…) × 5 formas (crua, sha256hex, SHA256HEX, sha256², digest cru) × 3 blobs KDF SHA256 + sha256(frase) como privkey. Nada.
- `primes_marker` (inline): composição de faed nas 104 posições primas (base 0 e 1) vs esperado — todos os 9 símbolos com |z| < 2 (g: 15/19,5 e 24/19,5); dbbi idem. As posições primas NÃO marcam um símbolo especial; "zerar os g nas posições primas" não tem suporte estatístico.
- `bifid3x3_exhaustive` (19,75 M, 0): TODAS as 60.480 classes de quadrados 3×3 × períodos × 2 modos em faed/dbbi (2,18 M saídas) como senha crua/sha256 nos 3 blobs, privkey e z-method; triagem invariante ao rótulo com nulo próprio. FECHADO. Ferramenta: `bifid3x3_pass2.py:try_pw` testa padding com 1 AES-ECB no último bloco (30× mais rápido) — usar em qualquer varredura futura.
- Também fechados na retomada: `select_bits` (3,0 M), `computed_lists` (5,2 M), `residue_autocorr` (1,1 M), `phase2_table_bytes` (799 k), `yinyang_interleave` (99 k), `pop_culture_exact` (102 k), `dutch_german_scorer` (23 k) — ver ENDGAME.md §8. O scorer de inglês NÃO é cego a nl/de (−5,45 em holandês real vs −6,66 embaralhado).
- `round3_inline` (R1/R2/R3/R5 do crítico, 1,19 M AES + 400 k privkeys + 704 z-methods, 0): R1 96 falas finais do Arquiteto/Neo × 11 grafias × 4 formas + roadmap composto; R3 3.638 linhas de comando openssl; R2 126 tokens (7 partes da fase 3 com genesis-hex e ambos FENs, casas/peças, fubcd/king/oracle/queen/thingky/mvps, tokens 3.2) em 1–3 partes (+4 no núcleo); R5 88 seletores de g→0 × 2 polaridades → z-method. Script: round3_inline.py (oráculo rápido de padding embutido).
- `symbol_map_variants` (R4, 1,65 M, 0): 14 mapas símbolo→valor × 9 fontes × 57 seleções × 26 materializações + janelas em todo offset; 3 controles plantados recuperados. Mapa global fixo FECHADO.
- `running_key_gate` (R6, 18.972 streams, 0): 51 running keys do corpus × 4 configs × 31 alinhamentos × 3 modos, gate |z|≥10 (controle z=−30,5, 0 falsos positivos). Só restam running keys EXTERNAS ao corpus. Regra: excluir chaves self-simétricas (faed rot 285 em sub/beau). Ferramenta: `running_key_gate.py:bigram_stats`.
- `external_running_key` (inline, 6,4 M streams, 0): OCR do livro Cosmic Duality como running key (a1z26/a0z25) em TODOS os alinhamentos × 4 configs × 3 modos, pré-filtro H_cond vetorizado + gate; único passer (z −10,5 com 30 emb.) cai a −5,4 com 3.000 emb. (nulo −7,8; controle −24,9); trecho = índice remissivo; decodes ruído. Regra: confirmar z de gate com ≥1.000 embaralhamentos. Scripts: external_running_key.py, erk_followup.py.
- `raising_variants` (inline, 6.360 oráculos, 0): parágrafo "Raising the stakes…" em 16 fatias × 31 variantes de whitespace/HTML/pontuação/caixa × 6 formas + concatenações 3.2/endgame. Lead do tail32_history fechado (sem o snapshot vivo do Wayback, que só um humano pode conferir).
